/**
 * mock server plugin for uba-server
 * Date : 2018-04-25 09:38:55
 */

const serveStatic = require("serve-static");
// const utils = require("./utils");

module.exports = (app, opt) => {
  //utils.loadMock(router, opt.file);
  
}
